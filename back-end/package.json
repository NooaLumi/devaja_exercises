{
	"name": "back-end",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "cross-env NODE_ENV=production node index.js",
		"watch": "cross-env NODE_ENV=development nodemon index.js",
		"build": "rm -rf build && cd ../front-end && npm run build --prod && cp -r build ../back-end"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"cross-env": "^7.0.2",
		"dotenv": "^8.2.0",
		"express": "^4.17.1"
	},
	"devDependencies": {
		"cors": "^2.8.5",
		"nodemon": "^2.0.3"
	},
	"engines": {
		"node": "12.x"
	},
	"heroku-run-build-script": false
}
